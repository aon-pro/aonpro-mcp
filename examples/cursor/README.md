# Cursor

Copy [`mcp.json`](./mcp.json) to `.cursor/mcp.json` in your project (or
`~/.cursor/mcp.json` for all projects) and replace `YOUR_AON_API_KEY` with an
issued live key from <https://developer.aon.pro>. Keep credentials out of
source control.

Then enable the server under **Settings → MCP**.
